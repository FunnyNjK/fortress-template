# `@fortress/testing`

Shared Vitest-aware factories/fixtures referenced from package and app suites.
Keep payloads obviously synthetic (`*_fixture`) so logs never collide with fixtures
from integrations.
